---
title: "Loading Swap"
description: "A component that swaps loading states with animations"
---





<Tabs items={["Preview","code"]}>
<Tab>
<ComponentPreview name="loading-swap-demo"/>
</Tab>
<Tab>
<ComponentSource name="loading-swap"/>
</Tab>
</Tabs>
## installation

### CLI

<Tabs items={["npm","pnpm","yarn"]}>
<Tab>
``` sh
npx shadcn@latest add https://dr-hooks.vercel.app/r/loading-swap.json
```
</Tab>
<Tab>
``` sh
pnpm dlx shadcn@latest add https://dr-hooks.vercel.app/r/loading-swap.json
```
</Tab>
<Tab>
``` sh
yarn dlx shadcn@latest add https://dr-hooks.vercel.app/r/loading-swap.json
```
</Tab>

</Tabs>

### Manual

<Steps>
  <Step>
    <strong>Install the following dependencies</strong>

    <Tabs items={['npm', 'pnpm','yarn']}>
      <Tab>
        ```sh
        npm install clsx tailwind-merge lucide-react
        ```
      </Tab>
      <Tab>
        ```sh
        pnpm add clsx tailwind-merge lucide-react
        ```
      </Tab>
      <Tab>
        ```sh
        yarn add clsx tailwind-merge lucide-react
        ```
      </Tab>
    </Tabs>
  </Step>

  <Step>
    <strong>If you don't have Tailwind configured yet, add it (dev deps)</strong>

    <Tabs items={['npm', 'pnpm','yarn']}>
      <Tab>
        ```sh
        npm install -D tailwindcss postcss autoprefixer
        npx tailwindcss init -p
        ```
      </Tab>
      <Tab>
        ```sh
        pnpm add -D tailwindcss postcss autoprefixer
        pnpm dlx tailwindcss init -p
        ```
      </Tab>
      <Tab>
        ```sh
        yarn add -D tailwindcss postcss autoprefixer
        yarn dlx tailwindcss init -p
        ```
      </Tab>
    </Tabs>
  </Step>

  <Step>
    <strong>Add a classname utility function</strong>

    ```tsx
    import { clsx, type ClassValue } from 'clsx';
    import { twMerge } from 'tailwind-merge';

    export const cn = (...inputs: ClassValue[]) => {
      return twMerge(clsx(inputs));
    };
    ```
  </Step>

  <Step>
    <strong>Copy and paste the component</strong>

    <ComponentSource name='loading-swap' />
  </Step>

  <Step>
    <strong>Update the import paths</strong>

    <p>Adjust import paths to match your project's folder structure.</p>
  </Step>

</Steps>